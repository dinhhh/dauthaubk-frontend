import { StyleSheet } from "react-native"

export const BaseStyles = StyleSheet.create({

  textHeader: {
    fontSize: 18,
    fontWeight: "bold",
    color: "#34675C",
  },

  textHighLight: {
    color: "#FA4848",
    fontWeight: "bold"
  }
})